<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        أذونات الـ SMS دي بتتشال بالكامل في نسخة "play" (راجع
        app/src/play/AndroidManifest.xml) عشان تتوافق مع سياسة Google Play
        للأذونات المقيدة (RECEIVE_SMS/READ_SMS محصورة تقريبًا في تطبيقات
        الرسائل الافتراضية). موجودة هنا فقط عشان نسخة "direct" اللي بتتوزّع
        بره المتجر.
    -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- إعادة جدولة تنبيه الدفعات القادمة بعد إعادة تشغيل الجهاز (الإنذارات
         بتتشال عند الـ boot). مفيش أي استخدام تاني للإذن ده. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".ExpenseApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="مصروفاتي"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTracker">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="مصروفاتي"
            android:theme="@style/Theme.ExpenseTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- بيتعطّل تلقائيًا في نسخة "play" (android:enabled يتجاوَز في manifest الفلافور) -->
        <receiver
            android:name=".receiver.SmsReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="99999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- الفحص اليومي للدفعات القادمة (اشتراكات/دوريات/أقساط). بيشتغل من
             غير ما التطبيق يكون مفتوح، وبيعيد جدولة نفسه بعد الـ boot. -->
        <receiver
            android:name=".receiver.PaymentReminderReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".receiver.ExpenseNotificationListener"
            android:label="تتبع المصروفات (إشعارات)"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>